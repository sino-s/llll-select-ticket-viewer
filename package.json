{
  "name": "llll-select-ticket-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/extract-select-ticket-data.mjs",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && gh-pages -d dist"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^24.12.3",
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
